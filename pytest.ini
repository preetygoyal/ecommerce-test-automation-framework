[pytest]
pythonpath = .
testpaths = api/tests ui/tests
addopts = -ra --strict-markers
markers =
    api: API tests (pytest + requests) against automationexercise.com/api_list
    ui: UI end-to-end tests (Playwright) against automationexercise.com
    smoke: fast subset safe to run on every push
